import { SyncScrollingDirective } from './sync-scrolling.directive';

describe('SyncScrollingDirective', () => {
  it('should create an instance', () => {
    const directive = new SyncScrollingDirective();
    expect(directive).toBeTruthy();
  });
});
