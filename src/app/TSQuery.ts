export class TSQuery {
  // public selectClass(doc: HTMLElement, selector: string): HTMLElement[] {
  //   // console.log(
  //   //   Array.prototype.slice.call(doc.querySelectorAll(selector)).length
  //   // );
  //   // console.log(selector);
  //   return Array.prototype.slice.call(doc.querySelectorAll(selector));
  // }
  // public selectClass2(doc: Document, selector: string): HTMLElement[] {
  //   return Array.prototype.slice.call(doc.querySelectorAll(selector));
  // }
  // public selectId(id: string): HTMLElement | null {
  //   return document.getElementById(id);
  // }
}
